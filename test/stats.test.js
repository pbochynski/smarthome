//var request = require('supertest');
//var should = require('should');
var regulator = require('../modules/regulator');
//var app = require('../app');


//describe('Regulator endpoint', function () {
//	before(function(done) {
//		regulator.connect(done);
//	})
//
//	it('should show stat', function (done) {
//		regulator.stats(pihome).pipe(process.stdout);
//	});
//
//});

//regulator.connect(function(){
//	var stream = regulator.stats('pihome');
//	stream.pipe(process.stdout);
//	stream.on('finish',_ =>{regulator.disconnect()});
//
//});